const {
    jwtSecretKey,
    FBSecretKey,
    FBClientId,
    TwitterId,
    TwitterSecretKey,
    googleId,
    googleSecretKey
} = require('./secret');

module.exports = {
    jwtSecretKey,
    FBSecretKey,
    FBClientId,
    TwitterId,
    TwitterSecretKey,
    googleId,
    googleSecretKey
};
